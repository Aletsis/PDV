namespace PDV.Domain.Enums;

public enum ClientType
{
    /// <summary>Cliente de menudeo — compra en volúmenes normales al precio público.</summary>
    Retail = 1,
    /// <summary>Cliente mayorista — accede a precios especiales por volumen.</summary>
    Wholesale = 2
}
